In my python file, I create a nested if statement where I check the status code of the url. If it's not 200 the first time, then I try again and if it still fails, then I exit the program. I then use BeautifulSoup to read response.text using html.parser. While true, I set data to soup.find_all to get all table rows in the class simpTblRow. I create an empty dictionary called query. Then I make a for loop where for i in data, I look for the symbol, name, price, change and volume in data. I then append all the stuff I found into query. I create a client set to MongoClient(), set db set to client['stock'] then collection to db['info']. I then do collection.insert_many(query) to insert all the data into my database.
I made several php files, they're all the same the only difference is how I sort the result. I have a php file for each specific column I want to sort. In my main php file, 31609560, I set client to new MongoDB\client, set the db to client->stocks and collection to db->info. I set result to collection and finding everything. In my html, I set table border to 1 so that the border is thicker. I then create a table row where each header references another php file that we go to if we select it. If we select the header then the data gets sorted based on the header. Inside my html, I have 2 nested foreach loops where I check the key value pair and if the key doesn't have an id, then I echo the value for that specific key. This is the same thing I do in all my other php files. 
